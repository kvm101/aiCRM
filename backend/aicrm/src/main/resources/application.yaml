spring:
  application:
    name: aiCRM

  autoconfigure:
    exclude:
      # OpenAI starter тягне EmbeddingModel; для pgvector лишаємо лише Google GenAI embeddings
      - org.springframework.ai.model.openai.autoconfigure.OpenAiEmbeddingAutoConfiguration
      # Тимчасово вимикаємо PgVectorStore, щоб при attach файлів не йшли embeddings/LLM виклики назовні
      # - org.springframework.ai.vectorstore.pgvector.autoconfigure.PgVectorStoreAutoConfiguration

  threads:
    virtual:
      enabled: true # Важливо для масштабування SSE-з'єднань MCP-сервера

  servlet:
    multipart:
      max-file-size: 32MB
      max-request-size: 36MB

  ai:
    vectorstore:
      pgvector:
        index-type: HNSW
        distance-type: COSINE_DISTANCE
        dimensions: 768
        initialize-schema: true

    mcp:
      server:
        enabled: true
        name: aicrm-mcp-server
        version: 1.0.0
        transport: SSE
        sse-endpoint: /mcp/sse
        message-endpoint: /mcp/message

    google:
      genai:
        api-key: ${GEMINI_API_KEY:AIzaSyDd-SxvOf5GLW75Y3Pr0sTbrLLDwyBp8ks}
        embedding:
          api-key: ${GEMINI_API_KEY:AIzaSyDd-SxvOf5GLW75Y3Pr0sTbrLLDwyBp8ks}
          text:
            options:
              model: gemini-embedding-2
              output-dimensionality: 768
        chat:
          options:
            model: gemini-2.5-flash

    # OpenAI auto-config вимкнено — ми керуємо бінами вручну (Groq, GitHub, Mistral)
    openai:
      api-key: "disabled"
      chat:
        enabled: false
      embedding:
        enabled: false

    retry:
      max-attempts: 2
      initial-interval: 3000
      multiplier: 2.0
      max-interval: 10000

    # Ключ для Groq (безкоштовний провайдер)
    groq:
      api-key: ${GROQ_API_KEY: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}
      
    github:
      api-key: ${GITHUB_TOKEN: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}
      
    mistralai:
      api-key: ${MISTRAL_API_KEY:LvAgUq6rbOlnempIEjMRB8t5aWU7tHy8}

  datasource:
    url: jdbc:postgresql://localhost:5432/mydatabase
    username: myuser
    password: secret
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: vasyl.karpliak.pp.2022@lpnu.ua
    password: qtwo ylfy fttu bgzr
    imap:
      host: imap.gmail.com
      port: 993
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: password

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: alpha
    tags-sorter: alpha

google:
  client:
    id: "998568744065-ssu9da6p7naqsk7mbvi6ahpk9djtimkj.apps.googleusercontent.com"
    secret: "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"
  redirect:
    uri: "http://localhost:8080/iam/oauth2/google/callback"
    system-uri: "http://localhost:8080/auth/oauth2/google/callback"

facebook:
  messenger:
    verify-token: ${FACEBOOK_VERIFY_TOKEN:my_super_secret_crm_token_2026}

aicrm:
  attachments:
    vector-indexing:
      enabled: true